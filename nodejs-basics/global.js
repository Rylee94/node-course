// global is nodes version of window
// node global.js
// console.log(global);

// global comes out of the box, don't need to say global.
// global.setTimeout(() => {
//   console.log("in the timeout");
// }, 3000);

// setTimeout(() => {
//   console.log("in the timeout");
//   clearInterval(int);
// }, 3000);

// const int = setInterval(() => {
//   console.log("in the interval");
// }, 1000);

// sends directory, and directory + filename
// console.log(__dirname);
// console.log(__filename);
